# Chapter 03 — panel findings applied (2026-09-02, from drafts/reviews/03-codex.md)

1. Lede rewritten: the two-year strategy is attributed to WIRED ("spend two years politely and enthusiastically volunteering to help"); the record sentence says an account using the name Jia Tan contributed for two years, became tagger and signer of releases, and signed the two tarballs (markers `03-tags-jiatan`, `03-collin-access`). No "he", "earning trust", "slipped", "chose" or "All of that holds". The break's folk-story paragraph now also quotes WIRED's "unlikely that Jia Tan is a real person" and says the commits "check the chronology, which is all they can check".
2. Reproducible-builds paragraph rewritten: "had run the backdoored library for weeks" and "had not tampered" removed; Cascadian's caveats quoted in his words ("may have at some point run the vulnerable xz-utils code", "not absolutely cleanest", "some sort of data point"); the result stated as sampled outputs matching the archive bit for bit despite those caveats, "evidence about the artefacts ... not proof about what the builders had run or had not done". "The morning after" → "Within a day"; "the people closest to it said so first" → "the practitioners' own posts show what it was used for instead".
3. Signature paragraph rewritten: "didn't spot any signs of committer fraud" is read as absence of detected fraud, not proof; the record establishes the commits carry the account's name; "Signing proves who built the artefact" replaced by: a valid signature shows a particular key signed a particular file and does not show who operated the key, who built the file, or whether it matches reviewed source. The defense section's "verified ... passed by design" sentence replaced with Collin's statement that the tarballs "were created and signed by Jia Tan".
4. Ledger row 1 narrowed to the two-year chronology ("Git dates the interval; it says nothing about intent") and graded HOLDS; intent moved to an OPEN row that quotes WIRED's "well-organized group" and Wikipedia's "sock puppetry" (merged with the former sock-puppet row to stay within 10 rows).
5. Tarball-vs-tree row re-sourced: Freund's opening line removed as the folk claim; the row now quotes WIRED ("added their stealthy backdoor to a version of XZ Utils") and the Wikipedia lede ("affecting both version 5.6.0 and 5.6.1"), with a word-presence check saved in `retellings-quoted-sentences.txt` showing WIRED's full text contains no "tarball" or "release" (claim `03-retelling-wordcheck`). Verdict stays MISSING.
6. Closing exercise rewritten to name each comparison: tarball vs tag lists undeclared generated inputs (build-to-host.m4 as a file with no git counterpart, not a "modified" file); only tarball vs an independently regenerated release isolates the altered macro; neither shows the payload; a history diff shows binary fixtures added and replaced but not their meaning. "Any of the three closes the gap", "exactly one modified macro file" and "no diff catches it" removed; the "record the diff at every bump" option dropped.
7. "Microsoft employee" removed from the graded claim; the Freund row now quotes WIRED's "lone Microsoft engineer" and Malka's "500ms performance regression" and states that Freund's post names no employer ("the row grades the delay, not the job"). "(every retelling)" replaced by Ars's "malicious install script that injected itself into functions used by sshd", with the saved word check showing Ars and Wikipedia never mention Landlock or a sandbox.
8. Raw git receipts saved: `xz-git-tags-and-url-commits.txt` (left uncommitted by the previous agent) is now indexed in SOURCES.md and cited by `03-tags-jiatan`, `03-url-move`, `03-jiatan-first-commit`, `03-jiatan-first-patch`, `03-commit-counts`, `03-collin-lastcommit`; appended to it on 2026-09-02: per-tag `BEGIN PGP SIGNATURE` counts for all nine tags, the two URL commits' stats not previously shown, `--committer --reverse` output, 2023 counts, last Collin commits, and the empty `git log --all -- m4/build-to-host.m4`. Prose now says each tag "names Jia Tan as tagger and carries a PGP signature block" (signatures were not verified against a key; the TSV says so).
9. "No stable release was affected" → "No Debian stable version was known to be affected at the time of the advisory"; TSV row quotes the DSA's "Right now ... are known to be affected".
10. Absolutes bounded: "nobody was measuring that gap" → "nothing in the collected record shows anyone measuring that gap before Freund"; "Nobody reads those" → "usually treated as generated noise"; "edited by hand" → "not what the tool produces: it carried a line 'not in the upstream source of build-to-host'"; "the one daemon on every server" → "a daemon that commonly faces the internet"; "Every one of those is a file no reviewer has read" → "A code review built on git never sees those files, because they are not in git"; "No one has been charged and no government has published an attribution" → a dated corpus limitation with marker `03-no-attribution` (SOURCES.md note rewritten to match). Also "thousands of lines of autoconf churn" → "dominated by regenerated autoconf output"; "skims as a typo" → "reads as a typo".
11. "an engineer with no security background" → "An engineer who, by his own description, was not a security researcher" (marker `03-freund-not-researcher` added to the lede).
12. "These are two maintainers and one distribution" → "That is one upstream project and one distribution".
13. The x86-64/gcc/GNU ld/Debian-or-RPM conditions moved from box five to box three, in their own paragraph with the configure description; box five now ends with Freund's "pre-authentication context" trace (new claim `03-freund-preauth`).
14. Ars Technica reading entry now carries its title and link; Wikipedia added to the folk-story reading line.

Extra corrections made while re-examining the ledger and register:
- The saved Wikipedia excerpt (2026-09-01) had "Microsoft employee" and "sock puppetry in a pressure campaign" as lede text, and the ledger quoted "through OpenSSH" and "performance regression" without a saved excerpt. Re-fetched via the MediaWiki API on 2026-09-02: those phrases are in §Background; the lede says "through OpenSSH", "deliberately included into the software in February 2024", "a campaign lasting years". Saved in full as `wikipedia-xz-utils-backdoor-2026-09-02.txt`; every Wikipedia citation now names the section.
- "a dozen commits with plausible titles" is now itemised (the eight reverted, the .gitignore change, two ifunc "fixes", the Landlock "fix" — twelve) and receipted.
- "He could, and did, grant a second person the right to cut and sign releases ... the commits were good for two years" → the co-maintainer email came "within the month" of the first complaint and the first signed tag nine months later; "Nothing in the record says he was wrong to at the time" is now tied to Collin's review marking January 2024 as the first preparing commit. "trust, which can be earned by anyone willing to spend two years" → "the credential that matters is that administrator's trust. Here the record shows it extended within months of a first patch, and the payload arriving two years in."
- The defense section's contingency sentence ("only under systemd") corrected to Freund's actual observation (did not appear at first outside systemd; depended on environment variables) and receipted as `03-freund-env`.
- OWASP framing sentence now says A03 "expands the old 'Vulnerable and Outdated Components' category", which is what the OWASP introduction says (it does not say "renamed").
- Verdict cells changed to `<td class="verdict v-holds">` form.
- All 50 TSV rows re-verified against the saved excerpts and re-stamped `checked-by:fable:2026-09-02`; sources now name the excerpt file in every row.
- Rejected by the consolidator (Pro 3, 4, 5) — left alone; the two paragraphs concerned are unchanged and still under the 150-word line.
