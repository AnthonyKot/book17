# Chapter 00 — ledger (written last, 2026-09-03)

One row per chapter: the headline folk claim each chapter tested, with that chapter's verdict. Documents are cited in the chapters. Recompute the tally before publishing:

    python3 - <<'PY'
    import re,glob,html,collections
    tot=collections.Counter()
    for f in sorted(glob.glob('chapters/[0-9]*.html')):
        cells=re.findall(r'<td[^>]*class="verdict[^"]*"[^>]*>(.*?)</td>',open(f,encoding='utf-8').read(),flags=re.S)
        tot.update(html.unescape(re.sub('<[^>]+>','',x)).strip().split()[0].upper() for x in cells)
    print(dict(tot), sum(tot.values()))
    PY

| # | Chapter and folk claim | Verdict |
|---|---|---|
| 1 | Krebs: an SSRF attack on the WAF reached the metadata service | OPEN |
| 2 | TNW: MongoDB "will no longer come with an unsafe configuration out of the box" as of 3.6 | OVERSTATED |
| 3 | WIRED: two years of polite volunteering | HOLDS |
| 4 | Wikipedia/LastPass: Adobe "has not salted them" | MISSING |
| 5 | Progress via TechTarget: the 31 May patch "fixed the issue" | OVERSTATED |
| 6 | The Guardian: Strava "gives away location of secret US army bases" | HOLDS |
| 7 | Bloomberg: "a single compromised password" | OVERSTATED |
| 8 | BBC: the "NotPetya ransomware attack" | WRONG |
| 9 | CBS: prevented "if Target had reacted to warnings" | OVERSTATED |
| 10 | Ars Technica: "a single line of misplaced code" | HOLDS |

Count: HOLDS 3, OVERSTATED 4, WRONG 1, MISSING 1, OPEN 1.
